<%= render partial: 'layouts/admin_header'%>

<center><h2 style="background-color:#E0E0E0; border-top: 2px solid black">Invoices</center></h2><br>

<div><class = "adminInvoicesIndex">
  <p> Invoices Index</p>
    <%@invoices.each do |invoice|%>
      <p>Invoice #<%=link_to invoice.id, admin_invoice_path(invoice)%></p>
    <%end%>
</div>