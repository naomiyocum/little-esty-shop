<%= render partial: 'layouts/merchants_header', locals: { 
  merch_name: @merchant.name,
  merch_id: @merchant.id 
} %>

<%= render partial: 'form', locals: {
  path: "/merchants/#{@merchant.id}/items/#{@item.id}",
  method: :patch,
  fill_in_name: @item.name,
  fill_in_description: @item.description,
  fill_in_unit_price: @item.unit_price,
  button_text: 'Update Item'
} %>