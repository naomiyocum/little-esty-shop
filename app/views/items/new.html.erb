<%= render partial: 'layouts/merchants_header', locals: { merch_name: @merchant.name, merch_id: @merchant.id }%>

<%= render partial: 'form', locals: {
  path: merchant_items_path(@merchant),
  method: :post,
  fill_in_name: nil,
  fill_in_description: nil,
  fill_in_unit_price: nil,
  button_text: 'Create Item'
} %>